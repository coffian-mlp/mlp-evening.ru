<?php

namespace Core;

/**
 * Маркер «текст этой ошибки предназначен пользователю» (MLP-261, AR6-1).
 *
 * Бросай UserError ТОЛЬКО с сообщением, которое можно показать в UI как есть
 * («Файл слишком большой», «Логин занят»). Любое другое исключение граница
 * HTTP считает системным: детали уходят в error_log, наружу — общий текст.
 */
final class UserError extends \Exception {}
