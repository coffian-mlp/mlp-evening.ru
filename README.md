# 🦄 MLP-Evening: Поняшный Вечерок

**MLP-Evening** — это легкая и уютная платформа для организации совместных киносеансов (стримов). Проект изначально заточен под просмотр *My Little Pony*, но подходит для любого контента.

Ключевая особенность — полная автономность на **PHP**. Здесь реализован **real-time чат на SSE (Server-Sent Events)**, который работает на обычном хостинге без необходимости поднимать Node.js, Redis или WebSocket-серверы.

Включает в себя умный генератор плейлистов (с учетом весов и истории), систему пользователей с ролями и административную панель.

## 🌟 Возможности

*   **Плеер:** Вставка стрима (iframe) с поддержкой Twitch, GoodGame и других.
*   **Чат (Zero-Dependency Realtime):**
    *   Мгновенная доставка сообщений через SSE.
    *   Синхронизация времени сервера и клиента.
    *   Защита от потери соединения и дубликатов.
*   **Пользователи и Роли:**
    *   **Пользователь:** Регистрация, профиль, аватарки, цвет ника. Может писать, редактировать (10 мин) и удалять (Soft Delete) свои сообщения.
    *   **Модератор:** Обладает правами удалять и восстанавливать *любые* сообщения в чате.
    *   **Администратор:** Полный доступ к Дашборду, управлению пользователями, базой серий и настройками сайта.
*   **Цитирование и Markdown:**
    *   Возможность цитировать несколько сообщений сразу (карточки).
    *   Поддержка **Markdown** (жирный, курсив, зачеркнутый, код, `> цитата`).
    *   Спойлеры `||текст||` (скрытый текст).
    *   Упоминания пользователей `@Никнейм` с подсветкой.
    *   Панель инструментов для быстрого форматирования.
    *   Переход к оригинальному сообщению по клику на карточку цитаты.
*   **Медиа и Файлы:**
    *   **Загрузка:** Drag & Drop, вставка из буфера (Paste), загрузка файлов (до 30 Мб).
    *   **Эмбеды:** Автоматическое превращение ссылок в картинки и видео (YouTube, Rutube, HTML5 Video).
    *   **Безопасность:** Видео по умолчанию без звука (Muted).
*   **Умный Плейлист:**
    *   Генерация плейлиста на основе весов (популярности).
    *   Поддержка двусерийников (они всегда идут парой!).
    *   Учет просмотренного.
*   **Дашборд Админа:**
    *   Управление ссылкой на стрим.
    *   Просмотр библиотеки серий.
    *   Управление пользователями (редактирование профилей, ролей, банхаммер).
    *   Ручное голосование и сброс статистики.
    *   История просмотров.

## 🛠️ Технологии

*   **Backend:** PHP 8.3 (С оптимизацией Opcache).
*   **Frontend:** HTML5, CSS3 (Flexbox), jQuery (локальный).
*   **Шрифты:** Локальные Open Sans и Philosopher (Google Fonts Free).
*   **Database:** MySQL 5.7.
*   **Container:** Docker & Docker Compose.

## 🚀 Как запустить (Локально)

1.  Убедитесь, что у вас установлен **Docker** и **Docker Compose**.
2.  Клонируйте репозиторий.
3.  Запустите проект одной командой:
    ```bash
    docker-compose up -d --build
    ```
4.  Сайт будет доступен по адресу: `http://localhost:8080`

### Пароли и Доступы
*   **MySQL:** Конфигурация лежит в `docker-compose.yml` и `config.php`.
*   **Сайт:** Регистрация открыта.
*   **Админка:** Доступна пользователям с ролью `admin`. Кнопка (гаечный ключ) появляется в меню после входа.

## 📁 Структура проекта

*   `assets/` — Стили (CSS), Скрипты (JS), Картинки (IMG) и Шрифты (Fonts).
*   `src/` — Ядро PHP (Классы `Auth`, `Database`, `EpisodeManager`, `ChatManager`).
*   `src/templates/` — Общие части HTML (header, footer).
*   `migrations/` — SQL файлы для миграций базы данных.
*   `.docker/` — Конфигурации для Nginx и PHP.
*   `api.php` — Точка входа для AJAX запросов (POST).
*   `chat_stream.php` — Точка входа для SSE потока (GET).
*   `dashboard.php` — Панель управления.
*   `index.php` — Главная страница.

## 💜 Разработка

Проект создан с любовью к дружбе и коду.
*Always remain faithful to your friends!*
